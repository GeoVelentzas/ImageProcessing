close all; clear all; clc;

I = im2double(imread('flowers.tif'));

[E,Z,M] = gdlog(I,3);

figure; imshow(I);      title('Original Imaged');
figure; imshow(M,[]);   title('Image filtered with Derivative of Gaussian');
figure; imshow(Z,[]);   title('Zero Crossings ��� �������������� �� LoG');
figure; imshow(E,[]);   title('������ �����');
